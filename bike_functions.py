from vehicle_functions import *


class Bike(Vehicle): #Inheritance

    def __init__(self, color, gears=6, handle_bar_type=2, basket=0):
        super().__init__(model, number_plate, color, wheels, doors, passenger)  # Runs the __init__ in parent class
        self.color = color
        self.gears = 6
        self.handle_bar_type = 2
        self.basket = 0

def pedal(self):
    return ('WEEEEEEEE this is hard work!')
def wheelie(self):
    return ('WOIIIII Lukatdis, I can do a wheelie!')
def chain_it(self):
    return ('My bike is now secure')
def play_music(self):
    return ('Badum tsdsss')